FLASK_ENV=development
FLASK_DEBUG=1

# Production: set FLASK_DEBUG=0 and a strong JWT_SECRET
JWT_SECRET=change_me_to_a_long_random_secret
JWT_EXPIRES_MINUTES=10080

# MongoDB: local or Atlas connection string
MONGO_URI=mongodb://localhost:27017/smart_study_scheduler

# Must match the frontend origin (e.g. https://yourapp.vercel.app when deployed)
CLIENT_ORIGIN=http://localhost:5173

# Optional; default 5000
# PORT=5000

